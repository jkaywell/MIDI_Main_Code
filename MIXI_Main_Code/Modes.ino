//set buttons to register a control change
 boolean button1;
array.add(button1);
array.buttons(buttons);

buttons[0].update() = !drumState[0]; if(drumState[0]){controlChange(0,1,127);} else{controlChange(0,1,0); Serial.println("activate_code");}}


switch(state){
  Serial.println(state);
case 0:
